@model Smart_Dairy_Manager.Data_model.MeatProduction
@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<style>
    .vaccine-table-container {
        max-width: 700px;
        margin: 50px auto;
        background: #f8f9fa;
        border-radius: 10px;
        box-shadow: 0 4px 10px rgba(0,0,0,0.1);
        padding: 30px;
    }

        .vaccine-table-container h2 {
            text-align: center;
            margin-bottom: 25px;
            color: #343a40;
            font-weight: 600;
        }

    table {
        width: 100%;
        border-collapse: collapse;
    }

    th {
        background-color: #007bff;
        color: white;
        padding: 12px;
        text-align: left;
        width: 35%;
    }

    td {
        padding: 12px;
        background-color: #ffffff;
        border-bottom: 1px solid #dee2e6;
    }

    tr:last-child td {
        border-bottom: none;
    }
</style>

<div class="vaccine-table-container">
    <h2>MeatProduction Details</h2>
    <table class="table">
        <tbody>
            <tr>
                <th>MeatProductionId</th>
                <td>@Model.MeatProductionId</td>
            </tr>

            <tr>
                <th>CowId</th>
                <td>@Model.CowId</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>@Model.Date</td>
            </tr>
            <tr>
                <th>CowWeight</th>
                <td>@Model.CowWeight</td>
            </tr>
            
        </tbody>
    </table>
</div>
 